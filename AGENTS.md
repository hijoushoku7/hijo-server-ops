# AGENTS.md

## このリポジトリについて

`hijo-server-ops`（バイナリ名 `hso`）— Minecraft サーバー用のラッパー型 TUI コンソール。**Linux 専用。**

詳細な仕様・技術調査・意思決定の経緯は [docs/spec.md](docs/spec.md) にある。**実装に入る前に必ず読むこと。**

管理 issue: hijoushoku7/life#24

## Claude の回答スタイル

- 挨拶・前置き禁止（「承知しました」等を書かない）
- 段階報告禁止
- 絵文字禁止
- 結論ファースト
- 指摘すべきことは遠慮なく指摘する
- 常に日本語で返答すること

## 技術スタック

- **言語**: Go
- **TUI**: Bubble Tea + Lipgloss
- **対象 OS**: Linux のみ（`/proc`・cgroup 依存）
- **ビルド**: `CGO_ENABLED=0 go build -ldflags="-s -w" -o hso ./cmd/hso`

## 設計上の原則

1. **ユーザーのファイルを書き換えない**。JVM フラグの注入は `JAVA_TOOL_OPTIONS` 環境変数経由で行い、`user_jvm_args.txt` や起動スクリプトには一切触れない。
2. **JVM 引数を管理しない**。`-Xmx` 等はユーザーの既存設定をそのまま使い、実効値は hsperfdata から読み取って表示するだけ。
3. **起動スクリプトをパースしない**。中身は任意の bash なので静的解析は破綻する。スクリプトごと子プロセスとして実行する。
4. **取れない情報は正直に `n/a` と出す**。推測値で埋めない（例: hsperfdata が読めない環境では `heap: n/a`）。
5. **ヒープと RSS は必ず両方出す**。片方だけでは誤診断を招く。`RSS - Heap committed` の差分がこのツールの核心的な価値。
6. **ヒープの瞬間値だけを見せない**。GC までゴミが溜まるノコギリ波なので、GC 直後の谷の値を併記する。

## やらないこと

- 複数サーバーの集中管理（Pterodactyl 等の領域）
- 既存の稼働中プロセスへのアタッチ（ラッパー型に限定）
- Windows 対応（v1 時点。優先度低）
- サーバーへのプラグイン導入を前提とする機能
